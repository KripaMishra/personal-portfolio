---
title: mastra-pii
summary: "PII redaction for Mastra agents as an adapter, not a detector: one interface over a Presidio deployment — the package owns integration, the deployment owns intelligence — with a deterministic Indian-recognizer layer and fail-closed defaults."
visibility: public
featured: true
order: 2
lifecycle: in-development
stateSince: 2026-08-09
currentState: "Live on npm as [v0.2.0](https://www.npmjs.com/package/@kripamishra/mastra-pii) (stable) with provenance attestation. One interface, two analyzers: the [RemotePresidioAdapter](https://github.com/KripaMishra/mastra-pii/blob/main/src/analyzer.ts) over a deployed Presidio container (spaCy NER + per-request Indian recognizers) and a zero-dependency deterministic engine that doubles as the outage fallback. Extension path: recognizer pass-through ([#25](https://github.com/KripaMishra/mastra-pii/issues/25)) — shipped. P2 hardening complete ([#19](https://github.com/KripaMishra/mastra-pii/issues/19)); no open issues."
constraints:
  - Synthetic examples only; never invent or reproduce real user text.
  - No engine-accuracy claims anywhere; the comparison harnesses in docs/evaluation/ are internal-only.
  - The package owns integration, the deployment owns intelligence — no claims that the adapter configures or gates NER/model layers.
  - No promises of obfuscation canonicalization or new built-in recognizers — detection extensions are recognizer pass-through; #8–#13 are closed as out of scope.
  - No claims of implemented local NER, restoration, telemetry, or structured-document redaction.
  - Redaction is one-way and fail-closed by design; ner/model layer requests throw.
  - The anti-overfitting corpus and the publishing runbook are local-only artifacts, not repo files.
domains:
  - privacy-engineering
  - agent-infrastructure
  - pii-redaction
tags:
  - privacy
  - redaction
  - mastra
  - adapter
  - fail-closed
techStack:
  - TypeScript
  - Mastra (>=1.57)
  - Presidio
  - Node.js
badges:
  - open-source
  - versioned
  - case-study
  - writing
sourceLinks:
  - label: GitHub repository
    url: https://github.com/KripaMishra/mastra-pii
    kind: source
  - label: Public README
    url: https://raw.githubusercontent.com/KripaMishra/mastra-pii/main/README.md
    kind: docs
releaseUrl: https://www.npmjs.com/package/@kripamishra/mastra-pii
releaseLabel: "@kripamishra/mastra-pii 0.2.0-alpha.4 · npm"
articleRefs:
  - reliable-pii-redaction-for-modern-agents
adrRefs:
  - sensitive-data-boundary
challengeRefs: []
decisionSlots:
  - title: Start the privacy boundary with deterministic local redaction
    summary: The first release stays deterministic, local, and explicit about failure instead of implying broader unsupported privacy coverage.
    status: published
    adrRef: sensitive-data-boundary
---

## Context

The package owns the integration; the deployment owns the intelligence. mastra-pii is an adapter over a Presidio deployment — not a detector — and its story is compatibility and ease of use, not engine accuracy.

## Problem

An agent loop touches sensitive text at many surfaces — prompts, tool inputs and outputs, traces, logs, memory — and every one of them needs redaction coverage. The consumer is an agent application whose users write Hinglish and share Indian identifiers (Aadhaar, PAN, UPI, IFSC, bank accounts); Western redaction libraries optimize for English prose and US formats, so off-the-shelf coverage was never the answer. The hard problem that remains is integration: wiring a redaction layer into a Mastra agent normally means custom code at every boundary. The adapter exists so that wiring is zero.

## Approach

1. **One interface, two analyzers.** `RemotePresidioAdapter` talks to a deployed Presidio container (spaCy NER by default, plus per-request Indian recognizers); `LocalFallbackAdapter` is the zero-dependency deterministic engine that takes over on outage. Swap deployments without changing app code.
2. **The deterministic layer ships with the package.** `INDIAN_DEFAULTS` — Indian regex recognizers with client-side Verhoeff (Aadhaar) and Luhn (cards) checksums, a curated entity allowlist, and shape post-filters — sent as per-request `ad_hoc_recognizers`, and runnable standalone with no deployment at all.
3. **Drop-in Mastra integration.** `pii.processor` implements Mastra's `Processor` interface — `inputProcessors: [pii.processor]` plus `outputProcessors` is the whole integration, with a third hook (`processLLMRequest`) covering every model call including tool continuations. Placeholders stay type-tagged (`[PAN_1]`); custom regexes run in time-bounded worker sandboxes.
4. **Fail-closed by default.** Outages degrade to the local engine (or `[REDACTION_FAILED]` in strict mode); raw PII never leaks.
5. **Layer boundaries explicit.** The package accepts only the deterministic layer; `ner` and `model` requests are rejected. Adding a model layer or custom recognizers is a deployment-side decision.

## Roadmap

The P2 hardening backlog ([#19](https://github.com/KripaMishra/mastra-pii/issues/19)) is **complete** (2026-08-14) — every ordered item shipped and the package reached stable `0.2.0` on npm. Built-in recognizer expansion ([#9](https://github.com/KripaMishra/mastra-pii/issues/9)–[#13](https://github.com/KripaMishra/mastra-pii/issues/13)) and obfuscation canonicalization ([#8](https://github.com/KripaMishra/mastra-pii/issues/8)) are **closed as out of scope** — detection extensions flow through recognizer pass-through, not new built-ins.

Resolved (all closed 2026-08-14):

- [x] [#14](https://github.com/KripaMishra/mastra-pii/issues/14) — Presidio adapter: retries with jitter + timeout config
- [x] [#25](https://github.com/KripaMishra/mastra-pii/issues/25) — Recognizer pass-through: user recognizers (Presidio shape) replace the adapter's recognizer set; custom patterns (local shape) run in both modes — the documented way to add detection
- [x] [#16](https://github.com/KripaMishra/mastra-pii/issues/16) — Observability: per-entity redaction counters
- [x] [#15](https://github.com/KripaMishra/mastra-pii/issues/15) — Agent-callable redactPII Mastra tool
- [x] [#17](https://github.com/KripaMishra/mastra-pii/issues/17) — Structured-document inspection (redact inside JSON/objects)
- [x] [#18](https://github.com/KripaMishra/mastra-pii/issues/18) — Mastra compatibility matrix (1.57 → latest 1.x) — final guard

The repo currently has **no open issues**.

## Evidence

The package is live on npm (provenance-attested), the [README](https://raw.githubusercontent.com/KripaMishra/mastra-pii/main/README.md) documents the full contract, and the repo is public — `src/` for the adapter internals, `deploy/` for the Presidio recipe. Comparison harnesses live in [`docs/evaluation/`](https://github.com/KripaMishra/mastra-pii/tree/main/docs/evaluation) for internal use; accuracy is the deployed analyzer's property, not this package's headline. Portfolio examples are synthetic fixtures only.

## Decisions

The [published ADR](/work/mastra-pii/decisions/sensitive-data-boundary) records the starting decision: deterministic local redaction with fail-closed behavior before broader model-backed promises. The pivot to a remote Presidio analyzer was gated on an internal spike before any package work.

## Outcome / current state

The package is live on npm (`0.2.0`, provenance-attested) with 67 passing tests and a clean-consumer verification gate. What it does not do is documented as loudly as what it does:

- Obfuscated formats still defeat every engine — a stated permanent limit, not a backlog item.
- Names and addresses are NER-only, deployment-side.
- International identifiers have no built-in recognizers; pass-through is the extension path.
- The model layer is explicitly out of scope.

Those limits are scope, not surprises.

## Next

The hardening backlog (#19) shipped in full and the package is at stable `0.2.0` — the ordered plan (retries with jitter, recognizer pass-through, observability, the agent-callable tool, structured-document inspection, Mastra compatibility matrix) is done, with no open issues remaining. Future work is deployment-side (NER/model layers) or new scope, tracked as new issues.
